# Development/test environment overrides
DEBUG=True
HOST=127.0.0.1
PORT=8000
# Use SQLite for local testing to avoid requiring MySQL
DATABASE_URL=sqlite:///./geologai_test.db
# Reduce pool size for SQLite
DB_POOL_SIZE=5
# Redis disabled for local tests
REDIS_URL=redis://localhost:6379
